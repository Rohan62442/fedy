{
	"icon": "btsync-user",
	"label": "BitTorrent Sync",
	"description": "Sync Anything You Want.",
	"category": "Apps",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root -s uninstall.sh"
		},
		"status": { "command": "test -e /opt/bittorrent-sync/btsync" }
	}
}
